@model IEnumerable<ModelAPIStore.Detalle_Ventas>

@{
    ViewBag.Title = "Inicio";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear nuevo registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.suspencion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_suspencion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Productos.nombre_producto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ventas.id_venta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.suspencion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_suspencion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Productos.nombre_producto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ventas.id_venta)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_detalle_venta }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_detalle_venta }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id_detalle_venta })
        </td>
    </tr>
}

</table>
